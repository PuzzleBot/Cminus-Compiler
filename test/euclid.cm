/* A program that uses Euclid’s algorithm to 
compute gcd */

int stuff (int de){
	return de;
}

void notFun(void){
	return;
}

int gcd ( int v ) {
    if (v == 0) 
        return u;
    else
        return gcd(v, u - u / v * v); /* u-u/v*v == u mod v */
}

void main (void) {
    int x;
    int y;
    x = input(); 
    y = input();
    output( gcd(x, y) );
}