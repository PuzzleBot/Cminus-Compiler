/* A program that uses Euclid’s algorithm to 
compute gcd (Bad function call)*/

int addToAll (int u, int v ) {
    if (v == 0) 
        return u;
    else
        return gcd(v, u - u / v * v);  /* u-u/v*v == u mod v */
}

void main (void) {
    int x[5];
    int y;
    x = input(); 
    y = input();
    output( gcd(x, y) );
}
