/* A program that uses Euclid’s algorithm to 
compute gcd (invalid function declaration: bad parameters - 2 errors)*/

int gcd (int u, int v; int 5 ) {
    if (v == 0) 
        return u;
    else
        return gcd(v, u - u / v * v);  /* u-u/v*v == u mod v */
}

void main (void) {
    int x;
    int y;
    x = input(); 
    y = input();
    output( gcd(x, y) );
}
