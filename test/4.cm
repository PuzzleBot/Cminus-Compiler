int recurse(int x){
    int y;
    if(x < 2){
        recurse(x+1);
    }
    output(x);

    return(y);
}

void main (void) {
    int number;

    number = 500;

    number = recurse(0);
    output(number);
}
