/*Prints the largest of two numbers*/

void main (void) {
    int x;
    int y;
    x = 6;
    y = 7;  /*Ok up to here*/

    x = 5 + 8 * 10;
    y = x + 4;

    if(x > y){
    	output(x);
    }
    else{
    	output(y);
    }

    output(5000);

    return;
}