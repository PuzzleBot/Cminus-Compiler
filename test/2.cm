/*If x > y, print x.
  Basic function calls*/
int addxy4(int x, int y){
	return (x + y + 4);
}

int add2(int t){
	return t + 2;
}

int complexOp(int x, int y){
	return add2(x - x / y * y);
}

void main (void) {
    int x;
    int y;
    x = 6;
    y = 7;  /*Ok up to here*/

    x = input();
    y = addxy4(10, 5);

    if(x > y){
    	output(x);
    }
    else{

    }

    output(y);

    x = complexOp(x, 2);
    output(x);

    return;
}